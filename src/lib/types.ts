export interface ThoughtFrontmatter {
	title: string;
	date: string;
	description: string;
	slug: string;
	published: boolean;
}

export interface ThoughtMeta {
	title: string;
	date: string;
	description: string;
	slug: string;
}
